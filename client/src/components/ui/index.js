/**
 * UI Component Barrel Export
 *
 * Import from '@/components/ui' instead of individual files:
 *   import { Button, Input, Modal, Avatar } from '@/components/ui';
 *
 * This keeps import lines clean across the codebase and makes
 * refactoring component locations trivial.
 */

export { Button }            from './Button';
export { Spinner, PageLoader } from './Spinner';
export { Input }             from './Input';
export { Avatar, AvatarGroup } from './Avatar';
export { Badge, StatusBadge } from './Badge';
export { Modal }             from './Modal';
export { Tooltip, IconButton } from './Tooltip';
